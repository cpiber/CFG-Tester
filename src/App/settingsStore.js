import React, { useState } from 'react';
import { createContainer } from 'unstated-next';

function useSettings(initialState = {}) {
  
}

export default const SettingsContainer = createContainer(useSettings);